name: flutter_create
description: Create flutter project from templates e.g github. A standalone solution to issue https://github.com/flutter/flutter/issues/15279
version: 1.0.4
homepage: https://github.com/odunboye/flutter_create

environment:
  sdk: ">=2.12.0 <3.0.0"

executables:
  flutter_create:

dependencies:
  path: ^1.8.0
  rename: ^2.0.1
  args: ^2.3.0

dev_dependencies:
  lints: ^1.0.1
  test: ^1.19.4
